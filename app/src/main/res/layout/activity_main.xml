<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.android.com.MainActivity">

    <TextView
        android:id="@+id/article_heading"
        style="@style/tv_mainActivity.articleHeading"
        android:text="@string/tv_activityMain_articleTitle" />

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/article_heading">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/article_subheading"
                style="@style/tv_mainActivity.articleSubheading"
                android:text="@string/tv_activityMain_articleSubtitle" />

            <TextView
                android:id="@+id/article"
                style="@style/tv_mainActivity.article"
                android:text="@string/tv_activityMain_articleText" />

        </LinearLayout>

    </ScrollView>

</RelativeLayout>